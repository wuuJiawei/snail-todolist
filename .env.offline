# Offline Mode Configuration
# Copy this file to .env to run the application in offline mode

# Storage Mode - set to 'offline' for local-only mode
VITE_STORAGE_MODE=offline

# Supabase Configuration (not required in offline mode, but needed to prevent build errors)
# These are placeholder values - they won't be used in offline mode
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-key

# In offline mode:
# - All data is stored locally in IndexedDB
# - No login required
# - No realtime sync features
# - Chat and sharing features are disabled
# - Works with Tauri desktop app
